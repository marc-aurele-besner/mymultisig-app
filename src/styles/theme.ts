import { createSystem } from '@chakra-ui/react'

// Create a minimal theme for Chakra UI v3
// TODO: Migrate full theme customizations to Chakra UI v3 API
const theme = createSystem()

export default theme
